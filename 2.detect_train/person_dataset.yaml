# Train/val/test sets as 1) dir: path/to/imgs, 2) file: path/to/imgs.txt, or 3) list: [path/to/imgs1, path/to/imgs2, ..]
path: C:\Users\Administrator\Documents\student_projs\reid\2.detect_train\person_data # dataset root dir，数据集根目录，使用绝对路径
train: images/train  # train images (relative to 'path') ，训练集图片目录（相对于path）
val: images/val  # val images (relative to 'path') ，测试集图片目录（相对于path）
test:  # test images (optional

# Classes，类别
names:
    0: person